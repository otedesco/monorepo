# Supabase Local Development Environment Variables
# 
# This file is a template. Run `pnpm env:setup` to create .env with actual values.
#
# After running `pnpm supabase:start`, the `env:add:supabase` script will
# automatically append Supabase credentials from `supabase status --output env`
#
# Required variables (added automatically by env:add:supabase):
# - SUPABASE_URL
# - SUPABASE_ANON_KEY
# - SUPABASE_SERVICE_ROLE_KEY (if available)

# Optional: Add any custom environment variables below
# DATABASE_URL=postgresql://postgres:postgres@localhost:54322/postgres
